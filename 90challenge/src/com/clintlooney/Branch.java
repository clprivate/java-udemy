package com.clintlooney;

import java.util.ArrayList;

public class Branch {
    private ArrayList<Customer> customerList = new ArrayList<Customer>();

    // Behavior
    private void addCustomer(String customerName, double initialTransaction) {
        customerList.add()
        // Customer name
            // Validate that name is not alread on record
            // Initial transaction amount
    }

    private void addTransaction(String customerName, double amount) {
        // Validate that target customer record already exists
        // Add transaction to existing customer record
    }

    // Show list of a branch's customers

    // Optional: Show list of each customer's transactions

    // Hint: Use autoboxing and unboxing in transactions list.
}
